"use client";
import { FormEvent, useState } from "react";
export default function ContactPage() { const [sent, setSent] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSent(true); } return <main className="route-shell"><p className="marketing-kicker">ROOTS / CONTACT</p><h1>Let’s talk.</h1><p className="route-lede">Send a message about the platform, research, or professional partnerships.</p>{sent ? <section className="success-message"><h2>Message received.</h2><p>Thank you. We will route your enquiry to the right team.</p></section> : <form className="contact-form" onSubmit={submit}><label htmlFor="name">Name</label><input id="name" required /><label htmlFor="contact-email">Email</label><input id="contact-email" type="email" required /><label htmlFor="message">Message</label><textarea id="message" rows={5} required /><button className="primary-button" type="submit">Send message</button></form>}</main>; }
